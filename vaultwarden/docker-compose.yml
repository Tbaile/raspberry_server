version: '3.5'

services:
  vaultwarden:
    image: 'vaultwarden/server:latest'
    container_name: vaultwarden
    env_file:
      - .env
    volumes:
      - data:/data
    networks:
      - nginx

networks:
  nginx:
    external: true

volumes:
  data: